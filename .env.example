# RoutineFlow — Environment Variables
# Copy to .env and fill in your values before running docker compose

# JWT secret key — must be at least 256 bits (32+ characters)
# Generate a strong key: openssl rand -hex 32
JWT_SECRET=change-this-to-a-random-256-bit-string-in-production
