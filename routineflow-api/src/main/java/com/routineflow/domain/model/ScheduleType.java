package com.routineflow.domain.model;

public enum ScheduleType {
    /** Task recurs on a specific day of the week (original behaviour). */
    DAY_OF_WEEK,
    /** Task recurs on a specific day of the month (e.g. every 25th). */
    DAY_OF_MONTH
}
